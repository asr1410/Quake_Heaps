#include <iostream>
#include <vector>
#include <queue>
using namespace std;

int main()
{
    vector<int> roots(1 + ceil(log2(10)));
    priority_queue<int> pq;
    cout << roots.size() << endl;
    return 0;
}